# Credenciais do PostgreSQL usadas pelo serviço "db" (docker-compose).
# Copie para .env na raiz para sobrescrever os padrões:  cp .env.example .env
# Se não criar o .env, o compose usa os valores padrão abaixo.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
